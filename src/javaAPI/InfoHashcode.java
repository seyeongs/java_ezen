package javaAPI;

public class InfoHashcode {
	/*Object.hasCode(); 메소드는 객체의 메모리 번지값을 이용해 정수 형태의 헤시코드를 만들어 리턴하여 객체를 식별하는데 이용된다 //new연산자가 실행되면 Object.hashCode 메소드는 16진수 주소값을 리턴하기 위해 자동으로 실행된다
	 필요에 따라 hashCode메서드는 오버라이딩을 통해 필드값을 리턴한다.
	 오버라이딩된 hashCode메서드는 new연산자와 함께 객체가 생성될 때 호출된다
	 오버라이딩 되지 않은 hashCode는 객체의 메모리 주소를 정수 형태로 반환단다
	 
	 */
	
}
